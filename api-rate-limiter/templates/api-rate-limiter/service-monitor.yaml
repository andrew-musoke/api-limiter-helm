apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    {{- include "apiRateLimiter.labels" . | nindent 4 }}
  name: api-rate-limiter
  namespace: api
spec:
  endpoints:
  - interval: 30s
    port: http
  namespaceSelector:
    matchNames:
    - {{ .Values.apiRateLimiter.namespace }}
  selector:
    matchLabels:
      {{- include "apiRateLimiter.labels" . | nindent 4 }}
    {{- with .Values.apiRateLimiter.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
