{{- if .Values.protectedApi.ingress.enabled -}}
{{- $fullName := include "apiRateLimiter.fullname" . -}}
{{- $svcPort := .Values.protectedApi.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: protected-api
  labels:
    {{- include "apiRateLimiter.labels" . | nindent 4 }}
    {{- with .Values.protectedApi.labels }}
        {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.protectedApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.protectedApi.ingress.tls }}
  tls:
    {{- range .Values.protectedApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.protectedApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
